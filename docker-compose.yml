version: '3.8'

services:
  blog:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./app:/app
    ports:
      - "8080:8080"
    command: sh -c "apk add --no-cache sqlite-dev && npm install --no-optional && npm run dev"
